# Kia Display Manager Configuration File
# /etc/kia/config
#
# This file controls the behavior of the Kia display manager.
# Lines starting with '#' are comments and are ignored.
# Configuration format: key=value (no spaces around '=')

# ============================================================================
# AUTOLOGIN SETTINGS
# ============================================================================

# Enable automatic login without password prompt
# When enabled, Kia will automatically log in the specified user
# Useful for kiosk systems or single-user workstations
# Values: true, false, yes, no, 1, 0, on, off
# Default: false
autologin_enabled=false

# Username for automatic login
# Only used when autologin_enabled=true
# Must be a valid system user
# Leave empty to disable autologin
# Default: (empty)
autologin_user=

# ============================================================================
# SESSION SETTINGS
# ============================================================================

# Default session to launch
# This session will be pre-selected in the session menu
# Must match a session name from /usr/share/xsessions/ or /usr/share/wayland-sessions/
# Common values: xfce, gnome, kde, sway, i3
# Default: xfce
default_session=xfce

# ============================================================================
# AUTHENTICATION SETTINGS
# ============================================================================

# Maximum number of failed login attempts before lockout
# After this many failed attempts, the user will be temporarily locked out
# Valid range: 1-10
# Default: 3
max_attempts=3

# Lockout duration in seconds
# How long to block authentication attempts after max_attempts is reached
# Set to 0 to disable lockout (not recommended for security)
# Default: 60
lockout_duration=60

# ============================================================================
# LOGGING SETTINGS
# ============================================================================

# Enable logging to /var/log/kia.log
# Logs include authentication events, errors, and session launches
# Recommended to keep enabled for security auditing and troubleshooting
# Values: true, false, yes, no, 1, 0, on, off
# Default: true
enable_logs=true
